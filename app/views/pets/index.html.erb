<h1>All Pets</h1>

<% @pets.each do |pet| %>
  <section id= 'pet-<%=pet.id%>'>
    <ul>
      <img src= <%= pet.image %>>
      <li>Name: <%= pet.name %></li>
      <li>Age: <%= pet.approximate_age %></li>
      <li>Sex: <%= pet.sex %></li>
      <% if @shelter %>
        <p><%= link_to "Edit #{pet.name}", "/shelters/#{pet.shelter.id}/pets/#{pet.id}/edit" %></p>
      <% else %>
        <li>Current Shelter: <%= pet.shelter.name %></li>
        <p><%= link_to "Edit #{pet.name}", "/pets/#{pet.id}/edit" %></p>
      <% end %>
      <p><%= link_to "Delete #{pet.name}", "/pets/#{pet.id}", method: :delete %></p>
    </ul>
  </section>
<% end %>

<% if @shelter %>
  <%= link_to 'Add New Adoptable Pet', "/shelters/#{@shelter.id}/pets/new" %>
<% end %>
